[package]
name = "zkp_mobile_logic"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]  # 👈 Zaroori: Android isay Library maanta hai

[dependencies]
jni = "0.21.1"           # 👈 The Bridge: Rust aur Java ki dosti karwane wala
