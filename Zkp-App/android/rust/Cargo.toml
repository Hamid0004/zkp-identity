[package]
name = "zkp_mobile"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# 🌉 Android Communication
jni = "0.21.1"

# 🧠 ZKP Engine (Plonky2)
plonky2 = "0.2.2"
plonky2_field = "0.2.2"

# 📦 Data Handling & Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
bincode = "1.3"
base64 = "0.21"

# 🦁 NEW: Integrity Check (Critical for Chunking)
crc32fast = "1.3" 

# 🔐 Passport Security (Crypto)
hex = "0.4"
sha2 = "0.10"
rsa = { version = "0.9", features = ["sha2"] }
rand = "0.8"

# 🛠️ Build Fixes (Dependency Pinning)
base64ct = "=1.6.0"
rayon = "=1.10.0"

# 📝 Logging
log = "0.4"
android_logger = "0.13"