[package]
name = "zkp_mobile"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# 🌉 Android Communication
jni = "0.21.1"

# 🧠 ZKP Engine
plonky2 = "0.2.2"
plonky2_field = "0.2.2"

# 📦 Data Handling (New & Old)
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
hex = "0.4"     # For Day 71 (Passport)
bincode = "1.3" # ✅ Added back for Old Logic
base64 = "0.21" # ✅ Added back for Old Logic

# 📝 Logging
log = "0.4"
android_logger = "0.13"

# 🚀 Performance (Fixed Version)
rayon = "=1.10.0"